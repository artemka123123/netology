version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    
    ports:
      - "3000:3000"
    
    depends_on:
      - mongodb
    environment:
      
      DATABASE_URL: ""
      NODE_ENV: development
      
    volumes:
      - .:/app